require 'spec_helper'

describe Github::Analyzer::Kpis do
  it 'has a version number' do
    expect(Github::Analyzer::Kpis::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
